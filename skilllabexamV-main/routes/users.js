// routes/users.js
const express = require('express');
const router = express.Router();
const User = require('../models/User');

// Add your user registration/authentication routes here

module.exports = router;
